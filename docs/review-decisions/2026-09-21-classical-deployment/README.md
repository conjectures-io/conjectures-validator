# Classical-pool deployment compatibility

The merged admission release `ff0002b0d0b4ee02d765334ce640310fe72d62f2` did not include three settings already active in production image `sha256:d4543fa242d8a7d0228f137f9890f4b6c5f6e2405efbd9875579ce75c3cae73a`: serial solution elaboration, forwarding `LEAN_NUM_THREADS=1` through the environment-clearing sandbox wrapper, and the 96 GiB comparator address-space limit. Preserve those live settings alongside the new statement and task pins. This makes the deployed source reproducible without reverting the large-proof runtime fixes.

The deployment checks distinguish claimable verification work from exhausted retries. Green 24 submission `352b539f-e4dd-42d1-b6bd-96d7b96876fc` has three attempts (the configured maximum), no worker lease, and a separate running verifier with read-only proof/task mounts. Its input files and image are retained. Three completed proofs await review and one approved proof awaits payout. Those records keep their original task hashes, reports, reward identities and locked amounts; none is migrated, retried, approved, cancelled or paid by this deployment.

This is a scoped exception to the generic empty-review/reward drain recommendation in `POOL.md`: all four affected targets survive with exactly matching statement/definition snapshots, and review/payout state is independent of the current task IDs. Preserve an archived copy of the previous validator, tasks, configuration and verifier image so the old verification remains reproducible. Pause new intake for the cutover, verify that no claimable job or live worker lease remains, then activate the paired API, worker and task releases together. The independent Green 24 run continues against its existing mounted files.

Validation before image staging: 26 focused admission, workspace, comparator and environment-boundary tests passed. The new sandbox test exercises both build/export-style environment allowlists and confirms that untrusted environment variables are not forwarded.
